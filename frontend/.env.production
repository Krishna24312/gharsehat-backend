# Production build is served by Flask on the same origin as the API, so the
# API base is relative (empty). `npm run dev` ignores this and defaults to
# http://localhost:5000 (see src/config.ts).
VITE_API_BASE_URL=
